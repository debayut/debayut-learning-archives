#include <iostream>

int main() {
    
    //  The const keyword specifies that a variable's value is constant
    // read only and cannot be changed after it has been initialized.

    const PI = 3.14159; // common convention is uppercase naming for constants

    return 0;
}